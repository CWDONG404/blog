# CWD 个人博客黑金银主题设计验收

## 验收对象

- 背景视觉来源：`screenshots/design-redesign/reference-deepseek-harness.png`
- 头像视觉来源：`screenshots/design-redesign/reference-avatar-noir.webp`
- 头像成品：`src/assets/avatar-cwd.webp`
- 桌面实现截图：`screenshots/design-redesign/after-desktop-1280x900.png`
- 移动实现截图：`screenshots/design-redesign/after-mobile-390x844.png`
- 背景同屏对照：`screenshots/design-redesign/comparison-harness-desktop.webp`
- 头像同屏对照：`screenshots/design-redesign/comparison-avatar-desktop.webp`
- 页面状态：`/#/`，桌面资料卡完整展示，移动端资料卡默认折叠。

## 尺寸与归一化

- DeepSeek Harness 官方页面截图为 1265×889；实现桌面截图为 1260×900，均来自 1280×900 浏览器验收视口。背景对照板保留来源宽度，并将实现截图裁至同高 889px，避免因页面高度差误判。
- 用户头像来源副本为 800×800 WebP；头像成品为完整方图等比缩放后的 512×512 WebP，没有再次裁切人物、服装、手部或红花。
- 头像聚焦对照板将来源和实现统一到 512×512；实现侧来自页面中实际 120×120 头像区域的放大，因此仅用于判断构图完整性，不用其放大后的清晰度评价源文件质量。
- 移动端 CSS 验收视口为 390×844；浏览器可见截图为 375×812，差值来自应用内浏览器滚动条与可见区。页面根节点实测没有横向溢出。
- 浏览器验收使用 CSS 像素比例，桌面覆盖 1280×900 与 1258×810，移动端覆盖 390×844。

## 全局视觉对照

- 字体与排版：展示标题使用系统无衬线字体栈，正文维持 400 字重；标题、导航和正文层级清楚，长中文与英文技术词正常换行。
- 间距与布局：保留模板的左侧资料卡、右侧内容面板和移动底部导航；1280×900 下标题与导航互不碰撞，1258×810 下侧栏底部仍在视口内。
- 颜色与令牌：界面强调色已从玫红完整切换为金色 `#E3C46F`、深金 `#B78938` 和银灰 `#DCE2E8`；未发现残留粉色或洋红 CSS 色值。
- 图片质量：头像使用本地 512×512 WebP；桌面显示 120/150px、移动显示 80px，`object-fit: contain`，无远程依赖、无拉伸、无裁切。
- 文案内容：关于、履历、项目、博客和技术栈内容没有因本轮视觉调整被改写或添加虚构信息。

## 聚焦区域对照

- 背景：实现借用了视觉来源的深蓝黑纵深、细网格、局部冷色雾光和穿行光带；为保持个人博客原有黑金识别度，品牌蓝只作为低透明环境光，交互强调统一使用金色和银色。这里是风格借鉴，不是对官方落地页布局的逐像素克隆。
- 动效：桌面端网格保持静态，光晕以 22 秒周期缓慢漂移，扫描线以 18 秒周期穿过底层；实测前后 `transform` 发生变化。移动端隐藏扫描线并静止光晕，`prefers-reduced-motion` 下停用全部环境动画。
- 头像：同屏对照确认原图人物、手部、服装与背后红花均完整进入头像框；旧的头肩裁剪资产已被替换。
- 卡片：主面板为近不透明冷黑玻璃，内容卡片使用银灰边界和金色图标；底层动效不会直接压在正文上，阅读对比度稳定。

## 对比历史与修复记录

### 第一次对照

- [P1] 头像仅保留头肩区域，与用户要求的“图片缩放完全展示”不符。
- [P1] 全站 `gold` 语义变量实际映射到 345–350° 玫红色，导航、图标、标题短线和交互边框均偏粉。
- [P2] 原背景是酒红、紫红光团和粒子，缺少 DeepSeek Harness 的深色网格、蓝黑纵深与克制光带结构。
- 修复：重新从完整方图生成 512×512 WebP；头像改为 `object-contain`；删除旧裁剪资源；重建黑金银令牌；将环境层收敛为网格、光晕和扫描线三层。

### 第二次对照

- 背景同屏对照显示：深蓝黑纵深、规则网格和横向光感已经建立；金色仅承担个人品牌强调，没有复制 DeepSeek 品牌色。
- 头像聚焦对照显示：完整构图一致，实际尺寸下边框、圆角和清晰度正常。
- 未发现仍需处理的 P0、P1 或 P2 问题。

## 响应式与功能检查

- 390×844：`/#/`、`/#/resume`、`/#/portfolio`、`/#/blog`、`/#/stack` 与文章详情均无页面级横向溢出；卡片保持单列，底部导航固定。
- 移动资料卡：展开按钮为 44×44px，展开后 `aria-expanded=true`，详情可见且页面无横向溢出。
- 1258×810：资料侧栏 `scrollHeight === clientHeight`，`overflow-y: visible`，底部约为 722px，没有内部滚动条。
- 项目截图：两张 1440×900 本地封面均以 `object-fit: contain` 显示；项目链接维持 `_blank` 与 `noopener noreferrer`。
- 动效与无障碍：桌面环境动效运行；移动端减少动效；系统减少动态效果偏好可停用动画。
- 浏览器错误与警告：0。

## 工程检查

- `pnpm lint`：通过。
- `pnpm typecheck`：通过。
- `pnpm build`：通过；生成 3 篇文章的 `rss.xml`。
- `git diff --check`：通过。

## 后续细节

- 当前没有阻塞交付的 P3 项。若继续精修，可单独调整环境光的亮度，但不建议增加更多粒子层或降低主面板不透明度。

final result: passed
